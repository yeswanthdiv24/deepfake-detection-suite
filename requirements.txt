# DeepFake Detection Suite - Requirements
# Install with: pip install -r requirements.txt

# HuggingFace ecosystem
transformers>=4.40.0
huggingface-hub>=0.23.0
accelerate>=0.30.0

# UI
gradio>=4.36.0

# Computer vision (likely already installed)
opencv-python>=4.9.0
Pillow>=10.3.0

# Audio processing
torchaudio>=2.0.0
librosa>=0.10.2
soundfile>=0.12.1

# Deep learning (likely already installed - CPU build)
torch>=2.0.0
torchvision>=0.15.0

# Numerical / scientific (likely already installed)
numpy>=1.26.0
scipy>=1.13.0
matplotlib>=3.8.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
timm>=1.0.0
